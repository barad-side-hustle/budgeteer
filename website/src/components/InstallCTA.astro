---
const base = import.meta.env.BASE_URL.replace(/\/$/, '');
---
<section class="install-cta budgeteer-fade-up">
	<div class="deco-l" aria-hidden="true"></div>
	<div class="deco-r" aria-hidden="true"></div>
	<div class="deco-ring" aria-hidden="true"></div>
	<div class="deco-cross" aria-hidden="true"></div>

	<div class="budgeteer-wrap inner">
		<h2 class="display">
			<span class="budgeteer-serif-ital">Ready to feel </span>
			<span class="budgeteer-sans-bold">organized?</span>
		</h2>
		<p>Free. Open source. Lives on your computer forever.</p>
		<div class="row">
			<a class="budgeteer-btn budgeteer-btn-primary" href={`${base}/install/mac`}>
				<svg width="14" height="14" viewBox="0 0 24 24" fill="currentColor" aria-hidden="true">
					<path d="M16.365 1.43c0 1.14-.46 2.27-1.22 3.1-.82.9-2.15 1.6-3.27 1.5-.15-1.1.43-2.27 1.18-3.07.8-.86 2.18-1.5 3.31-1.53z"/>
				</svg>
				Get for Mac
			</a>
			<a class="budgeteer-btn budgeteer-btn-ghost" href={`${base}/install/windows`}>
				<svg width="14" height="14" viewBox="0 0 24 24" fill="currentColor" aria-hidden="true">
					<path d="M0 3.5L9.5 2v9.3H0V3.5zm0 8.7h9.5V22L0 20.5v-8.3z"/>
				</svg>
				Get for Windows
			</a>
		</div>
	</div>
</section>

<style>
	.install-cta {
		padding: 6rem 0;
		background: var(--budgeteer-bg);
		text-align: center;
		position: relative;
		overflow: hidden;
	}
	.inner { position: relative; z-index: 1; }
	.display {
		font-size: clamp(2.25rem, 5vw, 3.5rem);
		line-height: 0.98;
		margin: 0 0 1rem;
	}
	.install-cta p {
		font-family: var(--budgeteer-font-sans);
		font-size: 1rem;
		color: var(--budgeteer-fg-muted);
		max-width: 24rem;
		margin: 0 auto 1.5rem;
	}
	.row {
		display: flex;
		flex-wrap: wrap;
		gap: 0.65rem;
		justify-content: center;
	}

	.deco-l, .deco-r, .deco-ring, .deco-cross {
		position: absolute;
		pointer-events: none;
	}
	.deco-l {
		top: 18%; left: 8%;
		width: 56px; height: 56px;
		border-radius: 50%;
		background: var(--budgeteer-vibrant);
	}
	.deco-r {
		bottom: 18%; right: 10%;
		width: 70px; height: 70px;
		border-radius: 50%;
		background: var(--budgeteer-pink);
	}
	.deco-ring {
		top: 25%; right: 22%;
		width: 40px; height: 40px;
		border: 3px solid var(--budgeteer-blue);
		border-radius: 50%;
	}
	.deco-cross {
		bottom: 30%; left: 18%;
		width: 22px; height: 22px;
	}
	.deco-cross::before, .deco-cross::after {
		content: '';
		position: absolute;
		background: var(--budgeteer-orange);
	}
	.deco-cross::before { width: 100%; height: 3px; top: 50%; transform: translateY(-50%); }
	.deco-cross::after { width: 3px; height: 100%; left: 50%; transform: translateX(-50%); }

	@media (max-width: 48rem) {
		.deco-ring, .deco-cross { display: none; }
		.deco-l, .deco-r { transform: scale(0.7); }
	}
</style>
