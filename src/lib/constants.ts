/**
 * Canonical GitHub repository for Budgeteer. Single source of truth for every
 * in-app link (sidebar, setup wizard, "open an issue", etc.). Matches the
 * git remote (origin = barad-side-hustle/budgeteer).
 */
export const GITHUB_REPO_URL = "https://github.com/barad-side-hustle/budgeteer";
export const GITHUB_ISSUES_URL = `${GITHUB_REPO_URL}/issues/new`;
